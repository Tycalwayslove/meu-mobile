# PaginationDots

轮播图和引导页的位置指示器；默认只读，显式 `interactive` 后提供 44×44 原生页码按钮、单一 Tab 停靠点、LTR/RTL/纵向方向键、压缩和整组禁用。`activeIndex` 始终由调用方控制，`onChange` 只发布下一页意图。

永久功能、Props、Events、SSR/hydration、兼容性与测试事实源见 [PaginationDots.docs.mdx](./PaginationDots.docs.mdx)，后续修改必须同步该文档。
