# Selector

卡片式原生 radio/checkbox 选择器，支持单选、多选、受控/非受控、只读、最大选择数、Field、FormData、reset、RTL 和移动 WebView。组件不会截断 20+ 选项，但大量选项应改用可搜索的 Picker 或 TreeSelect。

公开契约、状态、键盘、兼容边界与 V2 维护记录见 [Selector.docs.mdx](./Selector.docs.mdx)。
