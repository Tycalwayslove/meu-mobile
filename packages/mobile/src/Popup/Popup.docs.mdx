---
name: Popup
slug: popup
package: "@meu/mobile"
exports: [Popup]
status: verification
priority: P0
since: 0.1.0
implementedVersion: null
lastReviewed: 2026-08-29
figma:
  fileKey: 1EjHFQkfyM4FNsfIuhFF1v
  nodeId: "151:724"
storyIds: [feedback-popup--bottom, feedback-popup--top, feedback-popup--left, feedback-popup--right]
source: packages/mobile/src/Popup
---

# Popup

## 当前能力

- 四方向 Portal 浮层，支持受控/非受控 open、Mask、滚动锁、焦点圈定/恢复、Escape 和关闭按钮。
- 支持自定义 portal container、forceMount、safe area、initial/return focus ref。
- 关闭事件提供 `mask | escape | close-button` reason，退出动画结束后卸载。

## 何时使用

作为需要阻断背景交互的通用模态面板基础。业务级 Dialog、Picker、ActionMenu 应优先使用对应高层组件。

## 何时不要使用

非模态提示使用 Popover/Toast；复杂拖拽与 snap 使用 BottomSheet；不在 Popup 内重复实现路由、数据请求或表单状态机。

## 结构与状态

Portal layer → optional Mask → dialog panel → optional close button → body。状态为 closed、entering、open、exiting；视觉方向由 position 决定。

## 基础用法

```tsx
<Popup aria-label="筛选条件" open={open} onOpenChange={setOpen}>
  <FilterForm />
</Popup>
```

## 受控与非受控

受控使用 `open/onOpenChange`，非受控使用 `defaultOpen`。受控模式下关闭意图只回调，不自行改变 open；调用方必须提交新值。

## 表单集成

Popup 不持有表单值。关闭前是否丢弃草稿由组合组件决定；提交按钮应在表单内使用原生 submit。

## Props

API 由 `PopupProps` 生成。调用方必须二选一提供 `aria-label` 或 `aria-labelledby`；重要行为 Props 包括 container、mask、lockScroll、closeOnEscape、closeOnMaskClick、restoreFocus、initialFocusRef、returnFocusRef、forceMount 和 safeArea。

共享 `OverlayContainer`、`OverlayDismissReason` 与 `OverlayOpenChangeDetails` 类型由本页归档；具体组件可以扩展 reason，但不得改写基础 `mask | escape | close-button` 含义。

## Events

`onOpenChange(open, details)` 仅在组件请求变化时调用。当前关闭 reason 为 `mask`、`escape`、`close-button`；程序化 open 变化不反向发事件。

## Ref 与命令式方法

ref 指向 dialog panel。焦点控制优先使用 `initialFocusRef` 和 `returnFocusRef`，不要依赖内部 DOM。

## 键盘操作

| 按键            | 行为                       |
| --------------- | -------------------------- |
| Escape          | `closeOnEscape` 时请求关闭 |
| Tab / Shift+Tab | 在最上层 Popup 内循环焦点  |

## 触摸与手势

Mask 点击可选关闭；面板本身不定义拖拽。关闭按钮目标至少 44×44。V2 需验证 pointer cancel 与滚动边界。

## 无障碍

面板使用 `role="dialog"`、`aria-modal="true"` 并强制可访问名称。打开时圈定焦点并通过共享引用计数管理器把背景 siblings 设为 inert/aria-hidden；嵌套模态只暴露栈顶，自定义 Portal 容器会沿祖先路径隔离背景，关闭后精确恢复原属性。Popover focus branch、由 `aria-controls` 关联的 NumberKeyboard 与 Toast live region 保持可达；真实读屏浏览模式仍需真机验证。

## 动效

位置决定进入/退出方向，presence 层保留退出 DOM。V2 需冻结时长/缓动/中断规则，并在 reduced-motion 下即时或淡化切换。

## 主题与定制

公开定制为 position、maskOpacity、safeArea、close button、container、className/style 与全局 Token。z-index 由 overlay 层级统一治理。

## SSR 与 Next.js

Portal 在客户端挂载；服务端不得访问 document。V2 需逐项证明 closed/open/forceMount 的 hydration DOM 稳定。

## RTL 与国际化

关闭 label 从 ConfigProvider locale 获取，也可显式覆盖。left/right position 在 RTL 下仍表示物理方向；若产品需要 start/end，需在 V2 API 评审后增加，不能静默翻转。

## 浏览器与 WebView

重点验证 iOS body lock、软键盘/visual viewport、返回焦点、嵌套 Portal 和浏览器后退键组合。

## 边界情况与已知限制

- 当前 `aria-modal` 固定为 true，不适合非模态用法。
- 背景隔离只管理同一 Document；跨 iframe/ShadowRoot 仍需宿主显式分区。
- container 指向自定义文档或 Shadow DOM 的边界尚未声明支持。

## 性能预算

关闭且非 forceMount 时不保留内容；全局监听和 body lock 必须引用计数并在卸载时清理。

## 测试证据

- Unit: `Popup.test.tsx` 覆盖关闭原因、焦点、栈顶 Escape、嵌套背景隔离、自定义容器属性恢复与关联 NumberKeyboard；`overlayHooks.test.tsx` 覆盖 tabbable 顺序与 isolation 引用计数。
- Story interaction: `Bottom.play` 验证打开、可访问对话框、关闭和焦点恢复；`check-storybook-links` 本地验证 play-fn 为 80/80（按 Storybook title 计）
- Storybook 浏览器矩阵：本地扫描器在 390×844 视口验证 374 个 Story × 7 场景（Light、Dark、en-US、RTL、reduced-motion、200% 字体、forced-colors），共 2,618 个组合；有 play 的 Story 必须先返回 `storyFinished=success`，再运行 axe WCAG A/AA，结果为 0 违规、0 `pageerror`。
- 官网 Next 运行时：本地 production build 在 390×844 下逐页验证 68 个组件页的 Light/Dark（136 场景）；主题恢复、H1/预览/API 结构、React `pageerror` / `console.error`（含 hydration mismatch）和 axe WCAG A/AA 均通过。
- Bundle：Rollup tree-shaken ESM（React 与包 peer dependencies external）单一公开值 gzip 8278 B / 32768 B 预算，组合 Brotli 7329 B；共享样式由应用单独导入一次。
- SSR/hydration: 单元覆盖 Portal 基础；隔离 Next production 以服务端 open 状态在 Chromium/WebKit 完成组合 hydration，0 `pageerror`、0 `console.error`。
- RTL: 待补
- Reduced motion: 待补
- Mobile Chromium/WebKit E2E: 打开、focus trap、Escape/关闭、焦点恢复、scroll lock 与 action 链路通过；嵌套 isolation 已有 Unit，读屏浏览模式留待真机。
- Chromatic Light/Dark: Figma 与 stories 已有基线，待显式矩阵
- Real device: 待补

## V2 优化记录

### 待优化

- 补软键盘、安全区、退出中断、RTL 和真机证明。
- 为尚未逐项覆盖的关闭 reason 添加更细的 Story play 与 E2E。

### 已完成

- 已统一 Portal、Mask、受控模型、焦点、滚动锁、关闭原因与四方向样式；补齐栈顶 modal background isolation、自定义容器和非模态关联分支。

## 变更记录

| 日期       | 版本   | 类型 | 内容                      | 迁移说明                                     |
| ---------- | ------ | ---- | ------------------------- | -------------------------------------------- |
| 2026-08-28 | 0.1.0  | docs | 建立 V2 留存文档基线      | 无                                           |
| 2026-08-29 | 未发布 | fix  | 背景 inert 与嵌套模态隔离 | API 兼容；打开模态后页面背景不再进入可访问树 |
