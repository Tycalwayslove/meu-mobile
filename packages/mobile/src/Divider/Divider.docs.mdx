---
name: Divider
slug: divider
package: "@meu/mobile"
exports: [Divider]
status: verification
localVerification: complete
localGapIds: []
priority: P0
since: 0.1.0
implementedVersion: null
lastReviewed: 2026-08-29
figma:
  fileKey: 1EjHFQkfyM4FNsfIuhFF1v
  nodeId: "440:2"
storyIds:
  [
    layout-divider--default,
    layout-divider--with-content,
    layout-divider--start-aligned,
    layout-divider--end-aligned,
    layout-divider--vertical,
    layout-divider--long-content,
    layout-divider--right-to-left
  ]
source: packages/mobile/src/Divider
---

# Divider

## 当前能力

- 原生 `role=separator`，显式输出 horizontal/vertical `aria-orientation`。
- horizontal 支持无文案、start/center/end 文案；空数组/布尔值/空 Fragment 不生成伪文案结构，`0` 与空字符串仍是有意内容。
- 长文案可收缩并自然断行，字号、字族、字重、字距与行高完整使用 meta typography token。
- vertical 随所在 flex 行拉伸，文案只适用于 horizontal。
- 分隔线使用 border token，并在 forced-colors 下切换为系统 `CanvasText`。

当前状态为 `verification`，尚未完成真机、高对比与读屏商用验收。

## 何时使用

用于同一页面区域中相关内容组的静态视觉与语义分隔。当背景、标题或留白已经清晰表达分组时可省略。

## 何时不要使用

- 不用作可拖拽 resize handle、进度条或装饰性下划线。
- 不在每个列表项后机械添加；连续列表优先由 List 的 divider 规则统一处理。
- 不用 Divider 替代标题层级、Landmark 或足够留白。

## 结构与状态

horizontal 无文案时为容器加一条 line；有文案时为 line、content、line。start/end 只缩短逻辑起点或终点一侧的线。vertical 由容器背景绘制单线，不渲染 children。组件无交互状态。

## 基础用法

```tsx
<Divider>订单信息</Divider>

<div style={{ display: "flex", height: 40 }}>
  <span>价格</span>
  <Divider direction="vertical" aria-label="价格与库存分界" />
  <span>库存</span>
</div>
```

## 受控与非受控

不适用。Divider 不持有状态。

## 表单集成

不参与表单值、校验或提交，仅分隔视觉/语义区域。

## Props

`direction` 默认 horizontal；`align` 默认 center 且仅影响 horizontal 文案。children 为可选 ReactNode，vertical 不渲染 children；其余属性透传原生 div，可用 `aria-label` 给没有可见文案的关键分界命名。

## Events

只透传原生 div 事件，没有自定义事件。Divider 本身不应绑定点击或拖拽行为。

## Ref 与命令式方法

ref 指向外层 `HTMLDivElement`，只提供原生 DOM 能力。

## 键盘操作

| 按键 | 行为                           |
| ---- | ------------------------------ |
| 无   | 静态 separator 不进入 Tab 顺序 |

## 触摸与手势

不定义触摸或手势，也没有触控目标要求。需要可拖拽分栏时应使用独立组件和完整键盘替代操作。

## 无障碍

组件固定使用 separator 角色和准确 orientation。字符串或数字 children 会同步为默认 accessible name；复杂 ReactNode 不能安全推导名称，需要调用方传 `aria-label` 或 `aria-labelledby`。纯装饰性且语义冗余的分隔线可由调用方传 `aria-hidden=true`，但应先确认不会丢失内容结构信息。

## 动效

不提供内建动效，因此没有 reduced-motion 分支。

## 主题与定制

线条使用 `--meu-color-border`，文案使用 muted color、UI font 与 meta line-height，间距使用 spacing token。forced-colors 下线条与文案采用系统 `CanvasText`；内部 class 不是公共 API。

## SSR 与 Next.js

纯渲染组件，不读取浏览器全局，服务端和客户端输出一致。只有传入事件处理器时才需要 Client Component 边界。

## RTL 与国际化

start/end 依赖 flex 的逻辑主轴，在 RTL 中自动交换视觉侧；DOM 与读屏顺序保持 line、content、line。组件没有内建文案，children 与 `aria-label` 由调用方本地化。

## 浏览器与 WebView

依赖 flex、CSS custom properties、overflow-wrap 和 forced-colors media query。完整支持范围沿用 V2 兼容矩阵；不识别 forced-colors 的旧 WebView 会保留普通主题样式，不影响 separator 语义。

## 边界情况与已知限制

- `0` 与空字符串是有效 horizontal children；null、undefined、boolean、空数组和空 Fragment 按无文案处理。
- 字符串与数字文案可自动命名 separator；复杂 ReactNode 不会被序列化为名称。
- vertical children 不渲染，避免把横向标签误塞进 1px 结构；调用方应使用 `aria-label` 命名。
- 极窄容器中两侧 line 保留最小 token 宽度，长文案使用 `overflow-wrap:anywhere`；无断行规则的自定义 child 仍可能溢出。
- Divider 不提供 thickness、tone 或 dashed API；没有真实业务证据前不扩展。

## 性能预算

无状态、无 effect 和运行时测量。horizontal 最多生成 4 个 DOM 节点，vertical 只生成容器；正式发布前仍需记录样式增量 gzip。

## 测试证据

- Unit/SSR（本轮 7/7）: `Divider.test.tsx` 覆盖方向语义、vertical children、零值、空 ReactNode、原生属性与 ref；`Divider.ssr.test.tsx` 覆盖确定性 separator 标记、真实 hydrateRoot 根节点复用和 0 recoverable error
- Story interaction: `WithContent` 验证 separator 的可访问名称与方向；`check-storybook-links` 已验证全库 81/81 title 各至少一项关键 play，本 title 的 play 在本地扫描中返回 `storyFinished=success`。
- Storybook 浏览器矩阵：本地扫描器在 390×844 视口验证 490 个 Story × 7 场景（Light、Dark、en-US、RTL、reduced-motion、200% 字体、forced-colors），共 3,430 个组合；有 play 的 Story 必须先返回 `storyFinished=success`，再运行 axe WCAG A/AA，结果为 0 违规、0 `pageerror`。
- 官网 Next 运行时：当前 production build 在 390×844 下逐页验证 68 个组件页的 Light/Dark（136 场景）；主题恢复、H1/预览/API 结构、React `pageerror` / `console.error`（含 hydration mismatch）和 axe WCAG A/AA 均通过。
- Bundle：Rollup tree-shaken ESM（React 与包 peer dependencies external）单一公开值 gzip 1283 B / 32768 B 预算，组合 Brotli 1140 B；共享样式由应用单独导入一次。
- SSR/hydration: 共置测试直接验证 horizontal/vertical 语义与根节点复用；既有隔离 Next 16 场景存在，本轮未重复执行该全局场景
- RTL: `RightToLeft` Story 进入全库 RTL 矩阵；独立 layout contract 在 Chromium/WebKit 中断言 start 短线位于 RTL 视觉起点、长文案不溢出
- Reduced motion: 不适用；没有内建动效
- Mobile Chromium/WebKit E2E: `composes layout, icon action, custom portal and hidden accessible text` 在两种引擎断言 horizontal separator、内容名称与 `align=start`
- Chromatic Light/Dark: 默认、文案、对齐、vertical、长文本与 RTL Story 可构建，显式 Light/Dark 快照矩阵待补
- Real device: 待补

## V2 优化记录

### 待优化

- 统一发布批次完成 Windows High Contrast、VoiceOver 与 TalkBack 真机/系统验收记录；long content、RTL 与 vertical stretch 已有双引擎几何断言。
- 根据真实产品案例决定是否需要 thickness 或 tone；当前不新增推测 API。

### 已完成

- 补齐公开 TSDoc、separator 语义、自动文本名称、orientation、逻辑对齐与 vertical 内容边界。
- 修复数字 0 children 被误判为空，长文案允许收缩和断行。
- 增加 forced-colors 系统色降级以及 vertical、长文案、RTL Story。
- 在隔离 Next H5 中补 separator 名称、方向、对齐与 hydration 断言。
- 空 ReactNode 不再产生伪文案结构；meta 排版去除硬编码并增加共置 SSR/hydration 回归。
- 增加 Chromium/WebKit 长文案不溢出、RTL start 位置和 1px vertical stretch 等高几何证据。

## 变更记录

| 日期       | 版本   | 类型         | 内容                                                 | 迁移说明                            |
| ---------- | ------ | ------------ | ---------------------------------------------------- | ----------------------------------- |
| 2026-08-28 | 0.1.0  | hardening    | 完善语义、长文案、高对比、RTL 与测试证据             | 无                                  |
| 2026-08-29 | 未发布 | verification | 增加 Next H5 双引擎 separator/hydration 证据         | 无                                  |
| 2026-08-29 | 未发布 | hardening    | 空节点归一化、meta typography token 与共置 hydration | 空数组/空 Fragment 不再生成文案结构 |
| 2026-08-30 | 未发布 | verification | 增加 RTL、长文案与 vertical stretch 双引擎几何证据   | 无                                  |
