---
"@meu/mobile": minor
---

Add an accessible declarative Toast and a context-scoped `ToastProvider` / `useToast` FIFO command API with stable-id updates, pausable durations, async actions, safe-area positions, and isolated Next H5 coverage. Preserve locale and theme tokens when overlays portal to `document.body`.
