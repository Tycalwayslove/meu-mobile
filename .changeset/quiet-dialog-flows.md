---
"@meu/mobile": minor
---

Add accessible declarative Dialogs and a context-scoped `DialogProvider` / `useDialog` command API with async action loading, error handling, focus restoration, scroll locking, and alert / confirm promise flows.
