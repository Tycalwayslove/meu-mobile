---
"@meu/mobile": minor
---

Add Watermark with SSR-stable SVG text, multiline and image patterns, image fallback, staggered
geometry, theme-aware color, non-interactive semantics, and recoverable DOM tamper observation.
