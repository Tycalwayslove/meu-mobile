import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Introduction" />

# Meu Mobile

Storybook 是组件状态、交互和无障碍工作台。正式设计契约位于 `meu-design`，这里展示代码实际可交付的状态。

所有组件至少覆盖默认、禁用、错误、加载、深色主题和 200% 字体缩放中的适用状态。
